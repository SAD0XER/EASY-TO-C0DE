#include <stdio.h>
/*
compile = gcc + file_name + tab
execute = a + tab

These commands for C Programs.
*/
int main(){

    int a;
    printf("ENter the number: ");
   scanf("%d",&a);
    printf("%d",a);
    return 0;
}