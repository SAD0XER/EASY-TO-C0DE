#incluse < stdio.h>
int main()
int main(0
int i,n;
double arr[100];
pintf("Enter the number of elements(1 to 100):");
scanf("%d", &n);
for (i=0:i<n: ++i)
prin("Enter number%d:" i+1);
scanf("%l,&arr[j]);
for(i=1;i<n;++i)
{
    if (arr[0] < arr[i])
        arr[0] = arr[i];
}
printf("Largest Elements=%2lln",arc[0]);
return 0;
}